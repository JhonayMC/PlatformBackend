pip install fastapi psycopg2 sqlalchemy pydantic python-dotenv passlib bcrypt pyjwt
pip install uvicorn
python -m uvicorn mym_plataforma.main:app --reload 
python -m uvicorn mym_plataforma.main:app --port 8001 --reload
pip install bcrypt==4.0.1 passlib

pip install email-validator
pip install fastapi-mail
pip install pydantic-settings

